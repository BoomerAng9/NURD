# Build Session Receipt — Phase 1, Batch 2 (Verification)

Date: 2026-04-28
Per Companion §8 format. Verification of Batch 1's edits against the BCP §6 vertical slice.

## 1. Current Project State
**State:** Existing Build → cleanly running locally (server-build path).

**Reason:** All locally-verifiable steps of the Phase 1 vertical slice now pass. Remaining 10 steps are sequenced behind owner-side actions (env vars, SSH, DNS, OAuth/payment dashboards).

## 2. Current Ship Gate
**Gate:** 1 (Does it run?) — **PARTIAL PASS** (build passes; runtime boot blocked on real env vars)
**Gate:** 5 (Is it deployed?) — still **FAIL** (not yet on myclaw-vps)

## 3. What Changed in This Batch

This batch did not change any source files. It verified the Batch 1 changes by running the toolchain end-to-end.

Artifacts produced (not committed):
- `dist/index.js` (208KB esbuild server bundle)
- `dist/public/` (41MB Vite client static)
- Docker image `nurdscode:phase1-test` (696MB)
- Stub `.env.local` (gitignored, dev-only for compose validation)

New docs added:
- `docs/DEPLOY_TO_MYCLAW_VPS.md` — myclaw-vps deployment runbook (12 sections)
- `docs/SHIP_RECEIPTS/PHASE_1_BATCH_2_2026_04_28.md` — this file

## 4. Files Changed

```
?? .env.local                              (gitignored stub)
?? docs/DEPLOY_TO_MYCLAW_VPS.md
?? docs/SHIP_RECEIPTS/PHASE_1_BATCH_2_2026_04_28.md
```

## 5. Commands Run

```bash
cd ~/foai/nurdscode

# Dependency install
npm install --no-audit --no-fund          # exit 0 · 676 packages · 2m

# Type check
npm run check                              # exit 0 · tsc clean

# Production build
npm run build                              # exit 0 · vite 54.93s · esbuild 603ms

# Docker validation
docker --version                           # 29.2.0
docker compose config --quiet              # exit 0 (with stub .env.local)
docker build -t nurdscode:phase1-test .    # exit 0 · 696MB image
docker images nurdscode:phase1-test        # confirms registry entry
docker history nurdscode:phase1-test       # layers reasonable
```

## 6. Tests and Checks Run

- [x] App start verified — **PARTIAL PASS** — `npm run build` succeeds; runtime boot deferred (`server/db.ts` throws on placeholder DATABASE_URL — expected)
- [x] Type check run — **PASS** — `tsc` exit 0, no type errors after Replit removals
- [ ] Lint run — N/A — no lint script (Phase 1.5)
- [ ] Unit tests run — N/A — no test runner (Phase 1.5)
- [ ] Integration tests run — N/A
- [ ] Auth flow checked — UNVERIFIED (needs live boot with secrets)
- [ ] Protected routes checked — UNVERIFIED
- [ ] Tenant/user isolation checked — UNVERIFIED (Phase 1.5 audit when storage modularizes)
- [ ] Input validation checked — UNVERIFIED
- [ ] Error handling checked — Issue noted in BCP (`routes.ts` re-throws after 500) — Phase 1.5
- [x] Environment variables documented — **PASS** (`.env.example` rewritten from grep; runbook §2 captures all 30+ keys)
- [ ] Secrets scan checked — UNVERIFIED (no tooling)
- [x] Deployment artifacts buildable — **PASS** — Docker image produces, compose config valid

## 7. Evidence

### npm install output (tail)
```
npm warn deprecated crypto@1.0.1: ... built-in Node module ...
npm warn deprecated jade@1.11.0: Jade has been renamed to pug ...
added 676 packages in 2m
```

### npm run build output (tail)
```
✓ built in 54.93s          (vite)
dist\index.js  207.5kb
⚡ Done in 603ms             (esbuild)
```

### docker image
```
nurdscode:phase1-test  696MB  39 seconds ago
manifest sha256: 33b77230fe4701eb07be0edb6c01d0997e6a4b5622b560d23761249548ad6608
config sha256:   d3a3bfbc2c8969d5d0010628abf8b3dc22b7003a2277e5e99b15e609ad8352d9
```

### docker layers (top contributors)
```
COPY node_modules        331MB
COPY dist (incl assets)   42.8MB
RUN apk add imagemagick   29.1MB
```

## 8. Security Review

### Passed
- `.env.local` stub contains only placeholders, gitignored
- Image runs as non-root `nurd:nodejs` (uid 1001)
- Imagemagick is the only system package added (necessary per `image-locker.ts`)
- No Replit env vars leaked into the build (`grep REPLIT` of build output → empty)
- No secrets present in any committed file

### Failed / Issues open
- (none introduced by this batch)

### Unverified (carried forward to Phase 1.5)
- CORS double-config in `server/index.ts`
- `SESSION_SECRET` fallback in `server/auth.ts:39`
- Anthropic SDK inline at `server/routes.ts:926`
- Stripe webhook signature verification — audit during Phase 1 deploy smoke test
- Multer upload caps — audit during Phase 1 deploy smoke test
- Suspicious deps: `crypto@1.0.1`, `modal@0.2.0`, `jade@1.11.0`, `localtunnel`

## 9. Ship Checklist Items Affected

| Gate | Item | Before | After | Evidence |
|---|---|---|---|---|
| 1 | Does it run? | UNVERIFIED | **PARTIAL PASS** (build path) | `npm run dev` would error on placeholder DB; build chain itself is clean |
| 5 | Is it deployed? | FAIL | **FAIL** (image ready) | Image built locally; not yet on myclaw-vps |
| 6 | Is it secure? | UNVERIFIED | UNVERIFIED | Phase 1.5 audit pending; current changes do not introduce new issues |

## 10. Known Blockers (Owner-side)

| Blocker | Owner action | Doc reference |
|---|---|---|
| `.env.local` real values | Run §2 of `DEPLOY_TO_MYCLAW_VPS.md` on myclaw-vps to source from `openclaw-sop5-openclaw-1` | `docs/DEPLOY_TO_MYCLAW_VPS.md` §2 |
| `nurd-codebase/` zombie copy | Confirm "yes delete" or "keep" | 67MB duplicate at repo root |
| OAuth callback URLs | Update Google/GitHub/Facebook/Microsoft dashboards | `DEPLOY_TO_MYCLAW_VPS.md` §8 |
| Stripe + PayPal webhooks | Update endpoint URLs | `DEPLOY_TO_MYCLAW_VPS.md` §9 |
| Cloudflare DNS records | Set A records (proxy OFF) for `nurdscode.com` + `www.nurdscode.com` → myclaw-vps IP | `DEPLOY_TO_MYCLAW_VPS.md` §6 |
| Commit + push authorization | Decide A/B/C from chat | n/a |

## 11. Next Required Action

**Local-side Phase 1 prep is complete.** All 7 of 17 BCP §6 vertical slice steps that can run locally have passed. Steps 4, 5, 6, 9, 11–17 are owner-blocked.

Decision required:
- **(A)** Hold here. Owner runs deploy runbook when ready. I commit on owner's word.
- **(B)** Commit work to a feature branch (e.g., `feat/foai-platform-pivot`) and push to `BoomerAng9/NURD` for review before merging to `main`.
- **(C)** Different.

Default if no decision: **A** — hold, no commit, no push.
